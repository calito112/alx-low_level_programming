#include "main.h"

/**
 * _islower - is a function that checks for lowercase character
 *
 * @c: a character declearation
 *
 * Return: 1 and 0
 */

int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
